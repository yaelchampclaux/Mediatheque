{% extends 'base.html.twig' %}

{% block title %}Serie{% endblock %}

{% block body %}
    <h1>Serie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Numero</th>
                <td>{{ serie.numero }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ serie.titre }}</td>
            </tr>
            <tr>
                <th>Nbtomes</th>
                <td>{{ serie.nbtomes }}</td>
            </tr>
            <tr>
                <th>Info</th>
                <td>{{ serie.info }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_serie_index') }}">retour a la liste</a>

    <a href="{{ path('app_serie_edit', {'id': serie.id}) }}">modifier</a>

    {{ include('serie/_delete_form.html.twig') }}
{% endblock %}
