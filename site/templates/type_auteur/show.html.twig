{% extends 'base.html.twig' %}

{% block title %}TypeAuteur{% endblock %}

{% block body %}
    <h1>TypeAuteur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_auteur.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ type_auteur.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_auteur_index') }}">retour à la liste</a>

    <a href="{{ path('app_type_auteur_edit', {'id': type_auteur.id}) }}">Editer</a>

    {{ include('type_auteur/_delete_form.html.twig') }}
{% endblock %}
