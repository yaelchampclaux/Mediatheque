{% extends 'base.html.twig' %}

{% block title %}TypeOeuvre index{% endblock %}

{% block body %}
    <h1>TypeOeuvre index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Type</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for type_oeuvre in type_oeuvres %}
            <tr>
                <td>{{ type_oeuvre.id }}</td>
                <td>{{ type_oeuvre.type }}</td>
                <td>
                    <a href="{{ path('app_type_oeuvre_show', {'id': type_oeuvre.id}) }}">Visualiser</a>
                    <a href="{{ path('app_type_oeuvre_edit', {'id': type_oeuvre.id}) }}">Éditer</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">Il n'y a pas de correspondences</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_type_oeuvre_new') }}">Nouveau</a>
{% endblock %}
